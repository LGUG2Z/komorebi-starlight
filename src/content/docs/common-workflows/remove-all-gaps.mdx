---
title: Remove All Gaps
description: How to remove all gaps
---

If you would like to remove all gaps by default, both between windows themselves, and between the monitor edges and the windows, you can set the following configuration options:

```json
{
  "default_workspace_padding": 0,
  "default_container_padding": -1,
}
```
